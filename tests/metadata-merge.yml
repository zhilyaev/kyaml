labels:
  app: labels
  test1: test1
  override: l

metadata:
  labels:
    override: new
    test2: test2
    app: metadata

deployments:
  app:
    metadata:
      labels:
        env: prod
        app: app
